<h1>Users: {{usersCount}}</h1>

{{#link-to "users.create" class="btn btn-primary"}}Add user{{/link-to}}

<ul>
 {{#each model}}
 <li>
  {{#link-to 'user' this}}
    {{this.name}}
  {{/link-to}}
 </li>
  {{/each}}
</ul>

{{outlet}}
